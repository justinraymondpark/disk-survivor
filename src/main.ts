import './style.css'
import { runGame } from './game'

// Boot directly into the 3D Alt Title overlay as the default title screen
// while preserving original title code for fallback/debug.
runGame()
